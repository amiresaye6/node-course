// math object

console.log(Math);
console.log(Math.PI);

const area = 7.7;

console.log(Math.round(area)); // rounds to the nears number eithr bigger or smaller
console.log(Math.floor (area)); // rounds to the smaller number
console.log(Math.ceil(area)); // rounds to teh bigger number
console.log(Math.trunc(area)); // removes any dicimal values

// rendom numberes
const num = Math.random() // random number between  0 and 1

console.log(num)
console.log(Math.round(num)) // 0 or 1
console.log(num * 100) // form 0 to 100

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= adelShakalQuotes .title %> - Adel Shakal's Blog</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .navbar {
            background-color: #333;
            width: 100%;
            padding: 15px;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar .logo {
            font-size: 24px;
            font-weight: bold;
        }

        .navbar .nav-links a {
            color: #fff;
            text-decoration: none;
            margin: 0 10px;
            font-size: 16px;
        }

        .navbar .nav-links a:hover {
            color: #ff8c00;
        }

        .container {
            max-width: 800px;
            margin-top: 40px;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            text-align: left;
        }

        .back-button {
            display: inline-block;
            margin-bottom: 20px;
            font-size: 16px;
            color: #007bff;
            text-decoration: none;
            transition: color 0.3s;
        }

        .back-button:hover {
            color: #0056b3;
        }

        .blog-title {
            font-size: 32px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }

        .blog-content {
            font-size: 18px;
            color: #555;
            margin-bottom: 25px;
            line-height: 1.6;
        }

        .blog-footer {
            font-size: 16px;
            color: #777;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .blog-footer .author {
            display: flex;
            align-items: center;
        }

        .blog-footer .author-icon {
            margin-right: 8px;
            color: #ff8c00;
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar">
        <div class="logo">Adel Shakal's Blog</div>
        <%- include('./partials/navBar.ejs') %>
    </nav>

    <div class="container">
        <!-- Back Button -->
        <a href="/blogs" class="back-button"><i class="fas fa-arrow-left"></i> Back to Blogs</a>

        <!-- Blog Detail Card -->
        <div class="blog-title"><%= adelShakalQuotes.title %></div>
        
        <div class="blog-content">
            <%= adelShakalQuotes.content %>
        </div>
        
        <div class="blog-footer">
            <div class="author">
                <i class="fas fa-user author-icon"></i>
                <%= adelShakalQuotes.author %>
            </div>
            <div class="date">Posted on: <%= new Date().toLocaleDateString() %></div>
        </div>
    </div>

</body>
</html>
